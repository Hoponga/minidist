def launch(
    nproc_per_node: int,
    script: str,
    script_args: list[str] | None = None,
    master_addr: str = "127.0.0.1",
    master_port: int = 29500,
    run_id: str | None = None,
) -> int:
    pass
